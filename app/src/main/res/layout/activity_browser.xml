<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".BrowserActivity"
    android:background="@color/colorMainBg">

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <WebView
            android:id="@+id/webview"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <ProgressBar
        android:id="@+id/progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_gravity="top"
        android:indeterminate="false"
        android:max="100"
        android:progress="0"
        android:visibility="gone" />

    <FrameLayout
        android:id="@+id/hidden_handle"
        android:layout_width="48dp"
        android:layout_height="96dp"
        android:layout_gravity="bottom|start"
        android:clickable="true"
        android:visibility="gone"
        android:focusable="true">

        <ImageView
            android:id="@+id/hidden_handle_icon"
            android:layout_width="32dp"
            android:layout_height="64dp"
            android:src="@drawable/lens_blur"
            android:layout_gravity="center|start"
            android:contentDescription="@string/menu_handle" />
    </FrameLayout>

    <LinearLayout
        android:id="@+id/menuContainer"
        android:layout_gravity="bottom|start"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:elevation="4dp"
        android:layout_margin="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:background="@drawable/rounded_group_bg"
        android:clipToPadding="false">

        <LinearLayout
            style="?android:attr/buttonBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:clipToPadding="false">

            <ImageButton
                android:id="@+id/menuItemHome"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="48dp"
                android:minHeight="48dp"
                android:src="@drawable/home"
                android:contentDescription="@string/home"
                android:backgroundTint="@drawable/button_bg_tint"
                android:elevation="2dp"
                app:tint="@drawable/button_text_color" />

            <ImageButton
                android:id="@+id/menuItemBack"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="48dp"
                android:minHeight="48dp"
                android:src="@drawable/arrow_back"
                android:contentDescription="@string/back"
                android:backgroundTint="@drawable/button_bg_tint"
                android:elevation="2dp"
                app:tint="@drawable/button_text_color" />

            <ImageButton
                android:id="@+id/menuItemForward"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="48dp"
                android:minHeight="48dp"
                android:src="@drawable/arrow_forward"
                android:contentDescription="@string/forward"
                android:backgroundTint="@drawable/button_bg_tint"
                android:elevation="2dp"
                app:tint="@drawable/button_text_color" />

            <ImageButton
                android:id="@+id/menuItemRefresh"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="48dp"
                android:minHeight="48dp"
                android:src="@drawable/refresh"
                android:contentDescription="@string/refresh"
                android:backgroundTint="@drawable/button_bg_tint"
                android:elevation="2dp"
                app:tint="@drawable/button_text_color" />

        </LinearLayout>

        <Button
            android:id="@+id/menuItemPage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/go_to_page"
            android:textColor="@drawable/button_text_color"
            android:backgroundTint="@drawable/button_bg_tint"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:elevation="2dp" />

        <Button
            android:id="@+id/menuItemExternal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/external_browser"
            android:textColor="@drawable/button_text_color"
            android:backgroundTint="@drawable/button_bg_tint"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:elevation="2dp" />

        <Button
            android:id="@+id/menuItemConfiguration"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/configuration"
            android:textColor="@drawable/button_text_color"
            android:backgroundTint="@drawable/button_bg_tint"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:elevation="2dp" />

        <Button
            android:id="@+id/menuItemGrabber"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/grabber"
            android:textColor="@drawable/button_text_color"
            android:backgroundTint="@drawable/button_bg_tint"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:elevation="2dp" />

        <CheckBox
            android:id="@+id/menuItemHideButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/hide_menu_button"
            android:textColor="@color/colorTextGray2"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:elevation="2dp" />

    </LinearLayout>

</FrameLayout>